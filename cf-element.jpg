export const APP_CONFIG = Object.freeze({
  catalog: {
    // "local" = assets/data/products.json, "supabase" = Edge Function.
    mode: "local",
    localUrl: "./assets/data/products.json",
    functionUrl: ""
  },
  email: {
    // "mock" keeps the current prototype behaviour. Switch to "supabase" later.
    mode: "mock",
    functionUrl: ""
  },
  supabase: {
    // The anon/public key may be used in a browser. NEVER place a service_role key here.
    anonKey: ""
  },
  kiosk: {
    idleResetMs: 180000
  }
});
